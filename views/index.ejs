<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="./styles/style.css">
  <title>Blog Based Application</title>
</head>
<body>
  <h1>Create A New Blog and share it with the world.</h1>
  <h2 style="text-align: center;">Write a blog.</h2>
  <div class="box">    
      <form action="/submit" method="post">
      <label for="title">Title</label><br>
      <input type="text" id="title" name="title"><br><br>
      <label for="name">writing the blog</label><br>
      <textarea name="writingbox" placeholder=" " id="writingBox"></textarea>
      <!-- Submit Button Below -->
      <button type="submit">Submit</button>
    </form>
  </div>

  <div>
    <h2>All Blog Posts</h2>
    <% if (blogs && blogs.length > 0) { %>
      <% blogs.forEach(function(blog) { %>
        <div style="border: 1px solid #ccc; margin-bottom: 20px; padding: 10px;">
          <h3><%= blog.title %></h3>
          <p><%= blog.content %></p>
    
          <!-- Buttons for Update and Delete -->
          <div style="margin-top: 10px;">
            <form action="/blogs/<%= blog.filename %>/edit" method="GET" style="display:inline;">
              <button type="submit">Update</button>
            </form>
    
            <form action="/blogs/<%= blog.filename %>?_method=DELETE" method="POST" style="display:inline;">
              <button type="submit" onclick="return confirm('Are you sure you want to delete this blog?')" >Delete</button>
            </form>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <p>No blog posts yet.</p>
    <% } %>
  </div>
</body>
</html>